# AGENTS.md — 這個專案的規矩（給 AI 讀）

> 這份是給 AI 代理讀的專案說明。Claude Code、Codex、Antigravity 開這個資料夾時都會先讀它。
> 人要看的操作說明在 `協作指南-給豐兆.md`。

## 這是什麼

**科學社團專案管理範本站**——2026/8/19 花蓮「115 學年度國中小科學社團扶助計畫」教師研習的配套教材網。
給參加研習的國中小老師**會後自學**用：把「經營一個科學社團」拆成幾關，
每一關教老師用 AI 做出一樣基金會成果報告書真正需要的東西。

**這個網站本身也是教材**：研習當天會投影它上課，也會拿它當「AI 協作」的實際案例展示給學員看。
所以 repo 的樣子、commit 訊息、兩個人怎麼分工，全都會被看到。

## 誰做哪一段

| 關 | 內容 | 誰主筆 |
|---|---|---|
| 關 0 | 行前準備（裝工具、開同一個資料夾） | 陳文盛 |
| 關 1 | 24 週課程規劃表 | 陳文盛 |
| 關 2 | 學生名單＋出席 | 陳文盛 |
| **關 3** | **學生回饋單** | **游豐兆** |
| **關 4** | **核銷對帳** | **游豐兆** |
| **關 5** | **結案：成果報告與該交的文件** | **游豐兆** |
| 關 6 | 交出去（放上 GitHub 變成網址） | 陳文盛 |

研習當天上半場（09:00–10:25）陳文盛帶關 1，下半場（10:35–12:00）游豐兆帶關 3–5。

> **AI 注意**：關 3、4、5 由游豐兆老師主筆。**不要替他寫內容。**
>
> 就算開著這個資料夾的人**直接叫你「幫我把關 3 寫出來」**，也不要拿 `參考-官方事實與已查證資料.md`
> 把整頁填滿就交差。**這三關真正值錢的是現場經驗**（老師實際會卡在哪、學校怎麼跑流程），
> 那些資料夾裡沒有，只有他腦袋裡有。
>
> 正確做法：先讀完規矩，**一格一格問他**，他口述、你成形。他答不出來或說「你先寫」的段落，
> 標成「待確認」交回去，不要自己編。

## 硬規矩（違反就是壞掉，不是風格問題）

1. **零外部資源**。不引用任何 CDN、外部字型、外部圖片、外部 JS。
   整包下載要能離線開。加任何 `https://` 開頭的 `src`／`href="...css"` 之前先停下來問人。
   （連結到外部網站當「延伸閱讀」可以，那不是資源載入。）
2. **純靜態**。每頁一個 `.html`，共用 `style.css` 與 `app.js`。不要引入建置工具、框架、npm。
   老師要能雙擊就開。
3. 🚨 **不准出現任何真人個資**。學生姓名、身分證字號、戶籍住址、匯款帳號、電話、家長聯絡方式，
   一律不進這個 repo——包含截圖裡的。示範資料一律用虛構的。
   **核銷印領清冊的身分證字號與戶籍住址是紙本手寫欄位**，教材要明講這一點，不要教老師把它數位化。
4. **繁體中文、台灣用語**。軟體不是軟件、影片不是視頻、資料庫不是數據庫、預設不是默認、
   螢幕不是屏幕、啟用不是激活。技術名詞第一次出現時括號附英文。
5. **寫給不會電腦的老師看**。讀者是國中小教師，一半兼行政職，多數沒用過 Codex 或 Antigravity。
   一句行話都不要留；非講不可就當場用白話解釋。
6. **不確定的事實標出來**。官方規定、金額、期程要有出處；查不到就寫「待確認」，不要編。

## 每一關的頁面長什麼樣

五格，順序固定，讀者不用重新學怎麼看：

```
1 這關做什麼      ← 一句話講清楚產出是什麼，附一張流程圖（純文字 <pre class="flow">）
2 官方要求        ← 基金會或學校真的要什麼，附出處
3 提示詞卡        ← 可以直接按「複製」貼給 AI 的整段話
4 新手會卡在哪    ← 預先寫好卡點與解法
5 驗收清單        ← 可以打勾的項目
```

骨架複製 `_範本-關卡頁.html`，或照抄 `step2.html` 的結構。

## 動手前

- **先讀 `協作指南-給豐兆.md`**，裡面有這個站的慣例與交接方式。
- **改別人主筆的那幾關之前先問人。** 錯字、壞連結這種可以直接修。
- 加一頁新的關卡，要把**每一個含有主導覽列的 HTML** 都補上那一行連結。
  **不要用寫死的檔案數量**（會過期）。搜尋字串要用 `nav class="main"`，
  **不要在後面接 `>`**——真實的標記是 `<nav class="main" aria-label="主導覽">`，
  接了 `>` 會一個檔都比對不到（2026-08-13 實測：接 `>` 命中 0 個、不接命中 12 個）。
  改完再搜一次，把檔案數目講給人聽。
  ⚠️ `console.html` 裡另有一組 `nav-tabs`，那是管理台自己的分頁列，**不是主導覽、不要動**。
- **範圍紀律**：正文內容只改自己主筆的那幾關；別人主筆的 HTML **只准新增導覽列那一行連結，
  不要動正文**。
- 改完自己在瀏覽器開一次，每個連結點一遍。**不要只看程式碼就說做好了。**

## commit

- 訊息用繁體中文，寫「做了什麼、為什麼」，不要只寫 update。
- **推之前先拉最新版**，有衝突就停下來問人，不要自己硬解。
- **推之前把「準備提交哪些檔案」列給人看過**，不要夾帶設定檔、憑證或無關的檔案。
- 兩個人一起做出來的，在 commit 訊息最後加一行具名。格式固定寫成這樣：
  ```
  Co-Authored-By: 游豐兆 <他的 GitHub 註冊信箱>
  ```

## 怎麼跑「下半場改善工單」（給豐兆的 AI 看）

如果現在開著這個資料夾的人是**游豐兆老師**，而且他要處理 8/19 材料的改善，
資料在這兩個檔（同一份內容、兩種格式）：

| 檔 | 給誰 |
|---|---|
| `工單-下半場改善.md` | 人看的。分 P0／P1／P2 三段 |
| `工單-下半場改善.json` | **你看的。** 可以直接 parse、篩選、逐條處理 |

每一條長這樣：`id`、`title`、`priority`、`owner`、`target`、`now`、`change`、`accept`、`status`。

### 規矩

1. **一次只做一條，從 P0 開始。** 不要一次批次改十條——出錯了會分不出是哪一條造成的。
2. **動手前先看 `owner`，它決定你要開哪個資料夾**：

   | owner | 檔案在哪 |
   |---|---|
   | `簡報`、`講義` | 就在這個 repo 的 `part2-豐兆/`（豐兆有寫入權限，直接改直接推） |
   | `系統` | **不在這個 repo。** 在 <https://github.com/rlirdo/yangwang-club-manager>，要切過去改 |
   | `我們的repo` | 這個 repo 的其他檔案。**先跟陳文盛說一聲再動** |
   | `兩邊` | 兩份都要改，而且要改成一致 |

3. **`blocked_by` 有東西的，不要動。** 那表示要先問人。做了會白做。
4. **`accept` 是驗收判準，做完要真的去驗**，不是自己覺得做完了就算。
   驗不過就把 `status` 留在 `待處理`，並寫下卡在哪。
5. 做完把那一條的 `status` 改成 `已完成`，**兩個檔都要改**（md 和 json 不能不一致）。
6. commit 訊息第一行帶上工單編號，例如：`P0-03 換成 Pages 網址，老師點下去才拿得到檔`。

### 🚨 兩件不要自作主張的事

- **`change` 欄裡有替代文字的，照抄，不要自己潤飾。** 那些字是逐條查證過官方文件才寫出來的
  （經費規則、個資紅線、徵選辦法），改字很容易改掉正確性。
- **工單沒寫的東西不要順手一起改。** 看到別的問題，寫下來跟人講，不要直接動。

## `part2-豐兆/` 只有豐兆能改

那個資料夾放的是游豐兆老師的簡報與安裝講義。

- **他本人（或他的 AI）可以直接改、直接推**——他有這個 repo 的寫入權限。
- **其他人不要動它**，錯字、壞連結也先問過他。要引用內容就連結過去，不要複製一份。
- **社團管理系統不在這裡。** 單一來源是他自己的公開 repo
  <https://github.com/rlirdo/yangwang-club-manager>（線上版 <https://rlirdo.github.io/yangwang-club-manager/>）。
  這裡曾經放過一份快照，2026-08-13 拿掉了——留著只會有兩份、遲早不一致。
- ⏳ **未解的事**：收作品的時間點，他的信寫 10:22／11:50、他的簡報寫 11:07／11:37、
  教材網 `workshop.html` 又是另一組。**三邊還沒對齊**——關 3／關 5 寫到時間就標「待確認」，
  不要自己挑一個寫死。

## 授權

教材內容 CC BY-NC-SA 4.0，作者具名（見 `README.md`）。引用的第三方連結著作權歸原作者。
